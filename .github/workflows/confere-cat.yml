name: confere-cat
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  confere:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: python scripts/confere_cat.py
